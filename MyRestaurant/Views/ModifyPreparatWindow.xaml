<Window x:Class="MyRestaurant.Views.ModifyPreparatWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:MyRestaurant.ViewModels"
        xmlns:conv="clr-namespace:MyRestaurant.Convertors"
        Title="Adaugă Preparat" Height="400" Width="400">
    <Window.Resources>
        <conv:PreparateConvertor x:Key="PreparateConvertor" />
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Name -->
        <TextBlock Text="Nume preparat:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <!-- Price -->
        <TextBlock Text="Preț:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding Price, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <!-- Quantity Per Portion -->
        <TextBlock Text="Cantitate/Porție:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding QuantityPerPortion, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <!-- Total Quantity -->
        <TextBlock Text="Cantitate totală:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox Text="{Binding TotalQuantity, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="1" Margin="5"/>

        <!-- Category -->
        <TextBlock Text="Categorie:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox ItemsSource="{Binding CategoriList}" 
          SelectedValue="{Binding SelectedCategoryId, Mode=TwoWay}"
          SelectedValuePath="Idcategorie"
          DisplayMemberPath="NumeCategorie"
          Grid.Row="4" Grid.Column="1" Margin="5"/>

        <!-- Add Button -->
        <Button Content="Adaugă preparat"
                Grid.Row="5" Grid.Column="1"
                HorizontalAlignment="Right"
                Width="150"
                Margin="5"
                Command="{Binding AddPreparat}">
            <Button.CommandParameter>
                <MultiBinding Converter="{StaticResource PreparateConvertor}">
                    <!-- Name -->
                    <Binding Path="Name" />
                    <!-- Price -->
                    <Binding Path="Price" />
                    <!-- QuantityPerPortion -->
                    <Binding Path="QuantityPerPortion" />
                    <!-- TotalQuantity -->
                    <Binding Path="TotalQuantity" />
                    <!-- Category Id -->
                    <Binding Path="SelectedCategoryId" />
                </MultiBinding>
            </Button.CommandParameter>
        </Button>

        <!-- Error Message -->
        <TextBlock Text="{Binding ErrorMessage}" Foreground="Red" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
    </Grid>
</Window>
